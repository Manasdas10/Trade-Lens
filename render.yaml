services:
  - type: web
    name: trade-lens-platform
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    # Render exposes this port (running Streamlit) to the public web
    port: 8501
    envVars:
      - key: PORT
        value: 8501
