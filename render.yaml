services:
  - type: web
    name: trade-lens-platform
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    # Render exposes the port specified by the PORT environment variable below
    envVars:
      - key: PORT
        value: 8501
